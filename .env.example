# Local did:web resolution: map https://tabulas.eu/... to this origin so DID
# documents and credentials load from the static export / dev server.
NEXT_PUBLIC_DID_RESOLUTION_ORIGIN=http://localhost:3000
